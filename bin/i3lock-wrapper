#!/bin/sh

dest=$(mktemp --tmpdir i3lock-wrapper-XXXXXXXX.png)
trap "rm '$dest'" EXIT

scrot -d0 "$dest"
mogrify -modulate 80,100 -scale 10% -scale 1000% "$dest" 

#feh $dest
exec i3lock -i "$dest" "$@"
